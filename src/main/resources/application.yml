spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hr
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate:
      SQL: DEBUG